[phases.setup]
# Railway automatically installs python when requirements.txt is present.
# We just append nodejs to the environment so we can build the frontend.
nixPkgs = ["...", "nodejs_20"]

[phases.build_frontend]
# Install npm packages and build the dist folder
cmds = ["cd frontend-react && npm ci && npm run build"]
dependsOn = ["install"]
